name: Frontend Prod
on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "frontend/**"

jobs:
  build_and_deploy:
    uses: ./.github/workflows/frontend-deploy.yml
    permissions:
      contents: "read"
      id-token: "write"
    with:
      branch: main

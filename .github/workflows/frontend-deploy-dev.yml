name: Frontend Dev
on:
  workflow_dispatch:
  push:
    branches:
      - develop
    paths:
      - "frontend/**"

jobs:
  build_and_deploy:
    uses: ./.github/workflows/frontend-deploy.yml
    with:
      branch: develop

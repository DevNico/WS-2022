name: Backend Dev
on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "backend/**"

jobs:
  build_and_deploy:
    uses: ./.github/workflows/backend-deploy.yml
    with:
      branch: main
